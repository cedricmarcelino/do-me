<%= render partial: "shared/header", locals: {title: "Profile"} %>
<div class="flex flex-col grow p-4">
  <%= form_with(model: @user, class: "contents") do |form| %>
    
  <% end %>
</div>
<%= render partial: "shared/footer", locals: {tab: "settings"} %>